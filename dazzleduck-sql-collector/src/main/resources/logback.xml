<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Collector specific logging -->
    <logger name="io.dazzleduck.sql.collector" level="INFO"/>

    <!-- Reduce noise from HTTP client -->
    <logger name="jdk.httpclient" level="WARN"/>

    <!-- Reduce noise from Arrow -->
    <logger name="org.apache.arrow" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
