<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Shutdown hook ensures forwarder is flushed on JVM exit -->
    <shutdownHook/>

    <!-- Console appender for local viewing -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- DazzleDuck Log Forwarding Appender -->
    <appender name="LOG_FORWARDER" class="io.dazzleduck.sql.logback.LogForwardingAppender">
        <baseUrl>${DAZZLEDUCK_BASE_URL:-http://localhost:8081}</baseUrl>
        <username>${DAZZLEDUCK_USERNAME:-admin}</username>
        <password>${DAZZLEDUCK_PASSWORD:-admin}</password>
        <ingestionQueue>${DAZZLEDUCK_INGESTION_QUEUE:-log}</ingestionQueue>
        <minBatchSize>1024</minBatchSize>
        <project>*, CAST(timestamp AS DATE) AS date</project>
        <partitionBy>date</partitionBy>
    </appender>

    <!-- Exclude DazzleDuck internal log forwarding classes to prevent infinite loops -->
    <!-- Note: Only exclude Log* classes (LogForwarder, LogForwardingAppender, etc.), not demo -->
    <logger name="io.dazzleduck.sql.logback.LogForwarder" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="io.dazzleduck.sql.logback.LogForwardingAppender" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="io.dazzleduck.sql.logback.LogToArrowConverter" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="io.dazzleduck.sql.client" level="INFO" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="org.apache.arrow" level="WARN" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger - both CONSOLE and LOG_FORWARDER -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOG_FORWARDER"/>
    </root>

</configuration>
